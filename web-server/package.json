{
  "name": "wasm-demo-test-app",
  "version": "1.0.0",
  "description": "Demo Web App that runs WASM code.",
  "scripts": {
    "docker": "npx live-server --port=3000",
    "dev": "npx live-server --host=localhost --port=3000"
  },
  "author": "Stefan Pantic",
  "license": "MIT",
  "main": "index.html"
}
